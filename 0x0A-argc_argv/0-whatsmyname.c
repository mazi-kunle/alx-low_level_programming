#include <stdio.h>
#include "main.h"

/**
 * main- a function.
 * @argc: argument counter.
 * @argv: argument counter.
 * Return: int.
 *
*/

int main(int argc, char *argv[])
{
	if (argc == 1)
	{
		printf("%s\n", argv[0]);
	}
	return (0);
}
