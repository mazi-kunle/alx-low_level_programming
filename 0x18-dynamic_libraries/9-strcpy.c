#include "main.h"
/**
 * _strcpy- a function.
 * @dest: parameter.
 * @src: parameter.
 * 
 * Return: char.
*/
char *_strcpy(char *dest, char *src)
{
	if (*dest == *src)
	{
		_putchar('0');
	}
	return (dest);
}
